logging:
  pattern:
    console: "%clr([%d{MM-dd-yyyy HH:mm:ss}]){blue} %clr([%5p]) %clr([%15.15t]){cyan} %clr(%-40.40logger{39}){blue} %clr(:){red} %clr(%m){faint}%n"
spring:
  application:
    token:
    debug:
    locale:
  jpa:
    generate-ddl: "true"
    open-in-view: "false"
    hibernate:
      ddl-auto: "none"
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQL9Dialect"
  datasource:
    url:
    password:
    username:
