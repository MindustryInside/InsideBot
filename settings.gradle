rootProject.name = 'InsideBot'

include 'encoding'

if(file('src/legacy').exists()){
    include 'legacy'
    project(':legacy').projectDir = file('src/legacy')
}

// Test only
// includeBuild '../Discord4J'
